import React from "react";
import NumCell from "../NumCell";
import NumGrid from "../NumGrid";

const GamePlayScreen = () => {
    const pos = { ri: 0, ci: 0 };
    return <NumGrid></NumGrid>
};

export default GamePlayScreen;
